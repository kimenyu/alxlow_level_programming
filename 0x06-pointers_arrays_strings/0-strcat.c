#include "main.h"
/**
 * _strcat - concatenates two strings,
 * @dest: destination.
 * @src: source.
 * Return: the pointer to dest.
 */
int main(void)
{
	int a;
	int b;
	strcat(int a, int b);
	return 0;
}
